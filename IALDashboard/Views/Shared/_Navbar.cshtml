@{
    var actionName = HttpContext.Current.Request.RequestContext.RouteData.Values["action"].ToString();

    actionName = System.Text.RegularExpressions.Regex.Replace(actionName, "[A-Z]", " $0");
    if ((ViewBag.actionName) != null)
    {
        actionName = ViewBag.actionName;
    }
}

    <nav class="main-header navbar navbar-expand navbar-white navbar-light">
        <!-- Left navbar links -->
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
            </li>
            <li class="nav-item"> <a class="nav-link" href="#" role="button" style="color:black; font-weight:500;"> @actionName</a> </li>

        </ul>

        <!-- Right navbar links -->
        <ul class="navbar-nav ml-auto">
            <!-- Navbar Search -->
            <li class="nav-item">
                <a class="nav-link" href="~/User/ChangePassword" title="Change Password">
                    <i class="fas fa-key"></i>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="~/User/Logout" title="Sign Out">
                    <i class="fas fa-sign-out-alt"></i>
                </a>
            </li>
        </ul>
    </nav>
